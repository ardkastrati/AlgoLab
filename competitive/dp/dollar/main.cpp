#include <iostream>
#include <vector>
#include <utility>
#include <algorithm>

typedef std::vector<int> VI;
typedef std::pair<int, int> PII;
typedef std::vector<PII> VPII;


int main() {
	double money; std::cin >> money;
	int C_array[] = {5,10,20, 50, 100, 200, 500, 1000, 2000, 5000, 10000};
	VI C(C_array, C_array + 11);
	VI multiples(30001, 0);
	
	for(int i = 5; i < multiples.size(); i = i + 5) {
		for(int j = 0; j < 	
	}
	while(money > 0) {
		
		std::cin >> money;
	}
}
